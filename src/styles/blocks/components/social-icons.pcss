.social-icons {
  display: flex;
}

.social-icons__item {
  margin-right: 40px;
  &:last-child {
    margin-right: 0px;
  }
}

.social-icons__icon {
  height: 25px;
  width: auto;
  fill: $text-color;
}
