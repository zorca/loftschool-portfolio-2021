.section-welcome {
  position: relative;
  background: none;
}

.logo-text {
  font-size: 36px;
  font-weight: 800;
}

.welcome__down {
  display: flex;
  justify-content: center;
  margin-bottom: 2rem;
}
